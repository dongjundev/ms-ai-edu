[project]
name = "webapptest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "dotenv-python>=0.0.1",
    "streamlit>=1.46.1",
]
